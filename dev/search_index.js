var documenterSearchIndex = {"docs":
[{"category":"section","location":"vignettes/04_decomposition/#Decomposition:-catching-a-composition-driven-spillover","page":"Decomposition","text":"A difference-in-differences \"effect\" is only as clean as the treatment that defines it. When a single treatment label pools two different kinds of event, the pooled estimate can be positive and significant even when the effect of interest is exactly zero — because one sub-population carries a confound. The interference-DiD machinery here makes the treatment an explicit, decomposable estimand, so you can split the pooled effect by sub-population and see the problem instead of publishing it.\n\nThis vignette shows the failure and the fix on a synthetic data set, so it runs without any external data. (It mirrors a real case: estimating crime spillovers around cannabis-dispensary openings, where \"openings\" silently mixed genuinely new storefronts with pre-existing medical shops that merely added recreational sales on the legalization launch day. The pooled spillover looked like displacement; it was entirely the launch-day conversions.)","title":"Decomposition: catching a composition-driven spillover"},{"category":"section","location":"vignettes/04_decomposition/#A-two-type-DGP-with-zero-true-effect","page":"Decomposition","text":"We simulate units of two treated types plus never-treated controls:\n\n\"new\" units adopt at staggered later times; their true treatment effect is zero.\n\"conversion\" units all adopt at one early date (a \"launch\"), and they sit in higher-baseline areas that receive a post-launch shock unrelated to treatment — a confound. Their true treatment effect is also zero; the post-launch jump is not caused by the treatment.\n\nusing DidInterference, DataFrames, Random, Statistics\nRandom.seed!(1)\nN, T = 3000, 6\nu = rand(N)\nctype  = [x < 0.45 ? :control : x < 0.72 ? :conv : :new for x in u]\ncohort = [c == :control ? Inf : c == :conv ? 2.0 : rand([3.0, 4.0, 5.0]) for c in ctype]\nz      = randn(N)                                  # observed covariate\nbase   = [c == :conv ? 1.5 : 0.0 for c in ctype] .+ 0.3 .* z   # conversions in higher-baseline areas\n\nrows = NamedTuple[]\nfor i in 1:N, t in 1:T\n    Wt    = Int(cohort[i] <= t)\n    shock = (ctype[i] == :conv && t >= 2) ? 0.8 : 0.0   # confound: conv areas jump at the launch\n    Y     = base[i] + 0.1t + shock + 0.0 * Wt + randn() # TRUE treatment effect = 0 for everyone\n    push!(rows, (id = i, time = t, cohort = cohort[i], z = z[i], Y = Y, G = 0))\nend\ndf = DataFrame(rows)\nnothing # hide","title":"A two-type DGP with zero true effect"},{"category":"section","location":"vignettes/04_decomposition/#The-pooled-estimate-is-spuriously-positive","page":"Decomposition","text":"Pool every adopter together and estimate one effect:\n\nest(d) = did_int_staggered(d; yname = :Y, time = :time, id = :id,\n                           cohort = :cohort, exposure = :G, g = 0,\n                           covariates = [:z]).agg.simple\npooled = est(df)\nround.((pooled.estimate, pooled.ci[1], pooled.ci[2]), digits = 3)\n\nThe pooled ATT is positive and its 95 % interval excludes zero — even though we built the data with no treatment effect at all. A naive reader would report \"treatment raises Y.\" The doubly-robust adjustment for the covariate z does not save us, because the confound is a post-launch shock, not something z predicts.","title":"The pooled estimate is spuriously positive"},{"category":"section","location":"vignettes/04_decomposition/#Decompose-by-sub-population","page":"Decomposition","text":"Re-estimate on each treated type separately (dropping the other type, keeping the never-treated controls). New adopters are cohorts 3–5; conversions are cohort 2.\n\nnew_only  = est(df[df.cohort .!= 2.0, :])                              # drop conversions\nconv_only = est(df[(df.cohort .== 2.0) .| .!isfinite.(df.cohort), :])  # keep conv + controls\n(new = round(new_only.estimate, digits = 3),\n conversion = round(conv_only.estimate, digits = 3))\n\nThe decomposition exposes the truth:\n\nnew-only ≈ 0 — correct; new adopters have no effect;\nconversion-only ≈ 0.8 — the planted confound, recovered;\nthe pooled estimate was just an n-weighted blend of the two, landing positive and significant for the wrong reason.","title":"Decompose by sub-population"},{"category":"section","location":"vignettes/04_decomposition/#The-lesson","page":"Decomposition","text":"A pooled DiD/spillover estimate can be composition-driven: positive and significant because one sub-population's treatment timing is confounded, not because the treatment does anything. Whenever a treatment label aggregates heterogeneous events — new versus converted, voluntary versus mandated, early versus late — estimate the arms separately. If the pooled effect lives in one arm and vanishes in the other, the pooled number is telling you about who is in the group, not about the treatment.\n\nThe interference-DiD framework helps here precisely because it forces the treatment and exposure to be explicit objects you can subset and re-estimate — turning a hidden composition artifact into a visible, reportable one.","title":"The lesson"},{"category":"section","location":"reference/#API-Reference","page":"Reference","text":"Each function's docstring describes the API; an @example block immediately after shows it running with real output.","title":"API Reference"},{"category":"section","location":"reference/#22-base-case","page":"Reference","text":"","title":"2×2 base case"},{"category":"section","location":"reference/#Example","page":"Reference","text":"using DidInterference, DataFrames\nusing Random\nRandom.seed!(1)\n\nN = 600\ndf = DataFrame(\n    W = rand(0:1, N),\n    G = rand(0:1, N),\n    z = randn(N),\n    Y_pre = randn(N))\ndf.Y_post = df.Y_pre .+ 0.2 .* df.z .+ 1.5 .* df.W .+ 0.5 .* df.G .* df.W .+ randn(N)\n\nres = did_int_2x2(df;\n    yname = :Y_post, yname_pre = :Y_pre,\n    treat = :W, exposure = :G, g = 1,\n    covariates = [:z], trim = 0.01)\n(estimate = round(res.estimate, digits = 3),\n ci       = round.(res.ci, digits = 3))","title":"Example"},{"category":"section","location":"reference/#Count-outcomes:-family-:poisson","page":"Reference","text":"For count outcomes, family = :poisson estimates a multiplicative (rate-ratio) ATT at exposure g instead of the additive default — scale-invariant and defined with zeros.\n\nusing DidInterference, DataFrames, Random, Distributions\nRandom.seed!(7); N = 4000\nz = randn(N); W = Int.(rand(N) .< 0.5); G = Int.(rand(N) .< 0.5)\nYpre  = rand.(Poisson.(exp.(0.5 .+ 0.3 .* z)))\nYpost = rand.(Poisson.(exp.(0.5 .+ 0.3 .* z .+ 0.2 .+ 0.4 .* (W .* G))))\ndf = DataFrame(Ypre = float.(Ypre), Ypost = float.(Ypost), G = G, W = W, z = z)\nres = did_int_2x2(df; yname = :Ypost, yname_pre = :Ypre, treat = :W,\n                  exposure = :G, g = 1, covariates = [:z], family = :poisson)\nround(res.estimate, digits = 3)   # multiplicative ATT at g=1 (truth exp(0.4)-1 ≈ 0.492)","title":"Count outcomes: family = :poisson"},{"category":"section","location":"reference/#Dynamic-event-study","page":"Reference","text":"","title":"Dynamic event study"},{"category":"section","location":"reference/#Example-2","page":"Reference","text":"using DidInterference, DataFrames\nusing Random\nRandom.seed!(2)\n\nN, T_post = 500, 4\ndf = DataFrame(W = rand(0:1, N), G = rand(0:1, N),\n               z = randn(N), Y_pre = randn(N))\nfor k in 1:T_post\n    df[!, Symbol(\"Y_post_$k\")] =\n        df.Y_pre .+ 0.2 .* df.z .+ 1.5 .* df.W .+ 0.5 .* df.G .* df.W .+ randn(N)\nend\n\nres = did_int_dynamic(df;\n    yname_pre = :Y_pre,\n    ynames = [Symbol(\"Y_post_$k\") for k in 1:T_post],\n    treat = :W, exposure = :G, g = 1,\n    covariates = [:z], trim = 0.01)\nres.per_period","title":"Example"},{"category":"section","location":"reference/#Staggered-adoption","page":"Reference","text":"","title":"Staggered adoption"},{"category":"section","location":"reference/#Example-3","page":"Reference","text":"using DidInterference, DataFrames\nusing Random\nRandom.seed!(7)\n\nN, T = 1000, 5\ncohort = rand([2.0, 3.0, Inf], N)\nrows = NamedTuple[]\nfor i in 1:N, t in 1:T\n    W_t = Int(cohort[i] <= t)\n    G_t = rand() < 0.4 ? 1 : 0\n    z_i = randn()\n    Y   = 0.5 * z_i + 1.5 * W_t + 0.5 * G_t * W_t + randn()\n    push!(rows, (id = i, time = t, cohort = cohort[i],\n                 z = z_i, Y = Y, G = G_t))\nend\ndf = DataFrame(rows)\n\nres = did_int_staggered(df;\n    yname = :Y, time = :time, id = :id,\n    cohort = :cohort, exposure = :G, g = 1, covariates = [:z])\nfirst(res.per_cell, 5)","title":"Example"},{"category":"function","location":"reference/#DidInterference.did_int_2x2","page":"Reference","text":"did_int_2x2(data; yname, yname_pre, treat, exposure, g, covariates,\n            trim=nothing, alpha=0.05, family=:gaussian)\n\nDoubly robust direct ATT at exposure level g in the two-period common-adoption-timing case (Xu 2023). Mirrors the R did_int_2x2().\n\nArguments\n\ndata::DataFrame — wide-format data with pre- and post-period outcomes.\nyname::Symbol — post-period outcome column.\nyname_pre::Symbol — pre-period outcome column.\ntreat::Symbol — binary treatment indicator column (0/1).\nexposure::Symbol — exposure column (integer; we estimate at G == g).\ng — target exposure level.\ncovariates::Vector{Symbol} — covariate columns.\ntrim, alpha — see _dr_atte.\nfamily::Symbol — :gaussian (default, additive ATT) or :poisson (multiplicative ratio-of-ratios ATT for count outcomes).\n\nReturns\n\nNamedTuple with estimate, se, ci, n_treated, n_control, n_total, n_at_g, n_dropped, exposure_g, family, influence.\n\nExamples\n\nusing DidInterference, DataFrames, Random\nRandom.seed!(1)\nN = 800\ndf = DataFrame(\n    W = rand(0:1, N),\n    G = rand(0:1, N),\n    z = randn(N),\n    Y_pre  = randn(N),\n)\ndf.Y_post = df.Y_pre .+ 0.2 .* df.z .+ 1.5 .* df.W .+ 0.5 .* df.G .* df.W .+ randn(N)\n\nres = did_int_2x2(df;\n    yname = :Y_post, yname_pre = :Y_pre,\n    treat = :W, exposure = :G, g = 1,\n    covariates = [:z], trim = 0.01)\nres.estimate, res.ci\n\n\n\n\n\n","title":"DidInterference.did_int_2x2"},{"category":"function","location":"reference/#DidInterference.did_int_dynamic","page":"Reference","text":"did_int_dynamic(data; yname_pre, ynames, treat, exposure, g,\n                covariates, event_time=nothing, trim=nothing, alpha=0.05)\n\nDynamic event-study DR DATT under common adoption timing (Xu 2026, Section I). For each post-period outcome column in ynames, runs did_int_2x2 against yname_pre. Returns per-period estimates and a simple average aggregate whose SE stacks per-period influence functions (same units across periods).\n\nReturns\n\nNamedTuple with per_period::DataFrame (rows: eventtime, estimate, se, cilo, ci_hi) and agg::NamedTuple with the cross-period average.\n\nExamples\n\nusing DidInterference, DataFrames, Random\nRandom.seed!(2)\nN, T_post = 600, 4\ndf = DataFrame(W = rand(0:1, N), G = rand(0:1, N),\n               z = randn(N), Y_pre = randn(N))\nfor k in 1:T_post\n    df[!, Symbol(\"Y_post_$k\")] =\n        df.Y_pre .+ 0.2 .* df.z .+ 1.5 .* df.W .+ 0.5 .* df.G .* df.W .+ randn(N)\nend\n\nres = did_int_dynamic(df;\n    yname_pre = :Y_pre,\n    ynames = [Symbol(\"Y_post_$k\") for k in 1:T_post],\n    treat = :W, exposure = :G, g = 1,\n    covariates = [:z], trim = 0.01)\nres.per_period\n\n\n\n\n\n","title":"DidInterference.did_int_dynamic"},{"category":"function","location":"reference/#DidInterference.did_int_staggered","page":"Reference","text":"did_int_staggered(data; yname, time, id, cohort, exposure, g,\n                  covariates, pre_period=nothing, cohorts=nothing,\n                  times=nothing, trim=nothing, alpha=0.05)\n\nStaggered-adoption DR DATT with interference (Xu 2026, Section II). For each cell (c, t) with t >= c, restricts to S_M = {C = c OR C > t}, computes the DR DATT at exposure g with W = 1{C == c}, and aggregates using the joint-IF stacking that correctly accounts for shared units across cells.\n\nReturns\n\nNamedTuple with per_cell::DataFrame, agg (with simple, event_time, cohort sub-aggregates), influence::Vector{Vector}, cell_ids::Vector{Vector}, exposure_g, pre_period, alpha.\n\nExamples\n\nusing DidInterference, DataFrames, Random\nRandom.seed!(7)\nN, T = 1500, 5\n# Cohorts: half treated at t=2, quarter at t=3, quarter never\ncohort = rand([2.0, 3.0, Inf], N)\nrows = NamedTuple[]\nfor i in 1:N, t in 1:T\n    W_t   = Int(cohort[i] <= t)\n    G_t   = rand() < 0.4 ? 1 : 0\n    z_i   = randn()\n    Y     = 0.5 * z_i + 1.5 * W_t + 0.5 * G_t * W_t + randn()\n    push!(rows, (id = i, time = t, cohort = cohort[i],\n                 z = z_i, Y = Y, G = G_t))\nend\ndf = DataFrame(rows)\n\nres = did_int_staggered(df;\n    yname = :Y, time = :time, id = :id,\n    cohort = :cohort, exposure = :G, g = 1, covariates = [:z])\nres.per_cell\n\n\n\n\n\n","title":"DidInterference.did_int_staggered"},{"category":"section","location":"vignettes/01_getting_started/#Getting-Started","page":"Getting Started","text":"This vignette walks through the 2×2 base case of DidInterference.jl on a small synthetic lattice DGP. We simulate a binary direct effect of size 1.5 and a binary spillover at exposure level g = 1 of size 0.5, so the true DR DATT at G = 1 is 2.0. The estimator should recover this without bias.\n\nusing DidInterference\nusing DataFrames\nusing Random\nusing Statistics\n\nfunction simulate_2x2(; N = 2000, seed = 1)\n    Random.seed!(seed)\n    lon = rand(N) .* 10\n    lat = rand(N) .* 10\n    z   = 0.3 .* lon .+ 0.2 .* lat .+ randn(N)\n    W   = Int.(rand(N) .< (1 ./ (1 .+ exp.(0.5 .- 0.6 .* z))))\n    dij = [sqrt((lon[i]-lon[j])^2 + (lat[i]-lat[j])^2) for i in 1:N, j in 1:N]\n    A   = (dij .< 1.5) .& (dij .> 0)\n    deg = max.(sum(A, dims = 2)[:], 1)\n    share = vec((A * W) ./ deg)\n    G = Int.(share .> median(share))\n    Y_pre  = 0.8 .* z .+ randn(N)\n    Y_post = Y_pre .+ 0.2 .* z .+ 1.5 .* W .+ 0.5 .* G .* W .+ randn(N)\n    DataFrame(W = W, G = G, z = z, Y_pre = Y_pre, Y_post = Y_post)\nend\n\ndf = simulate_2x2(N = 2000, seed = 1)\nfirst(df, 5)","title":"Getting Started"},{"category":"section","location":"vignettes/01_getting_started/#Single-fit","page":"Getting Started","text":"res = did_int_2x2(df;\n    yname      = :Y_post,\n    yname_pre  = :Y_pre,\n    treat      = :W,\n    exposure   = :G,\n    g          = 1,\n    covariates = [:z],\n    trim       = 0.01,   # Xu (2026) uses 0.01 in the Brazil application\n)\n\n(estimate = round(res.estimate, digits = 3),\n se       = round(res.se,       digits = 3),\n ci       = round.(res.ci,      digits = 3),\n truth    = 2.0)\n\nThe estimate sits close to the truth of 2.0 within roughly one standard error.","title":"Single fit"},{"category":"section","location":"vignettes/01_getting_started/#Small-Monte-Carlo:-bias-and-coverage","page":"Getting Started","text":"Repeating across 100 replicates to check that the bias goes to zero and the influence-function SE matches the empirical standard deviation across reps.\n\nreps  = 100\ntruth = 2.0\nests  = Float64[]\ncovs  = Bool[]\nfor r in 1:reps\n    d   = simulate_2x2(N = 1500, seed = r)\n    out = did_int_2x2(d;\n        yname = :Y_post, yname_pre = :Y_pre,\n        treat = :W, exposure = :G, g = 1,\n        covariates = [:z], trim = 0.01)\n    push!(ests, out.estimate)\n    push!(covs, truth >= out.ci[1] && truth <= out.ci[2])\nend\n\n(bias         = round(mean(ests) - truth, digits = 3),\n empirical_sd = round(std(ests),          digits = 3),\n coverage_95  = round(mean(covs),         digits = 3))\n\nThe empirical standard deviation of the point estimates and the influence-function-based mean SE typically agree to within sampling noise. Coverage of the 95 % CI should be close to the nominal level.","title":"Small Monte Carlo: bias and coverage"},{"category":"section","location":"vignettes/01_getting_started/#Optional:-trim","page":"Getting Started","text":"trim = 0.01 drops units whose cohort or exposure propensity score falls below 0.01 or above 0.99. This is the same trimming Xu uses in the Brazil application and substantially shortens the right tail of the estimator under poor overlap.\n\nres_notrim = did_int_2x2(df;\n    yname = :Y_post, yname_pre = :Y_pre,\n    treat = :W, exposure = :G, g = 1,\n    covariates = [:z])     # no trim\n\n(estimate   = round(res_notrim.estimate, digits = 3),\n n_dropped  = res_notrim.n_dropped)\n\nWithout trimming, individual replicates with units near the propensity-score boundary can produce noisy estimates.","title":"Optional: trim"},{"category":"section","location":"#DidInterference.jl","page":"Home","text":"Difference-in-differences with spatial interference, in Julia. Companion to the R package didint.\n\nImplements the doubly robust direct ATT estimators of:\n\nXu, Ruonan (2023). \"Difference-in-Differences with Interference.\" arXiv:2306.12003.\nXu, Ruonan (2026). \"Dynamic Difference-in-Differences with Interference.\" AEA Papers and Proceedings 116: 58–63.","title":"DidInterference.jl"},{"category":"section","location":"#Why","page":"Home","text":"Standard DiD assumes one unit's outcome does not depend on another unit's treatment. This is often the wrong assumption for spatial policies. Xu's setup keeps the conditional-parallel-trends comparison but adds an exposure mapping G_it = G(i W_-it) for unit i's neighborhood treatment status.","title":"Why"},{"category":"section","location":"#Three-estimators","page":"Home","text":"did_int_2x2 — two-period, common adoption (Xu 2023, 2×2 case).\ndid_int_dynamic — event study with common adoption timing (Xu 2026 §I).\ndid_int_staggered — staggered adoption with not-yet-treated comparison groups, joint-IF aggregation across cells (Xu 2026 §II).\n\nAll three use the same ingredients: three propensity models, two outcome-change regressions, and the doubly robust plug-in formula. Standard errors come from the empirical influence function. The optional trim argument matches Xu's Brazil application.","title":"Three estimators"},{"category":"section","location":"#Install","page":"Home","text":"using Pkg\nPkg.add(url = \"https://github.com/xiangao/DidInterference.jl\")","title":"Install"},{"category":"section","location":"#At-a-glance","page":"Home","text":"using DidInterference, DataFrames\n\nres = did_int_2x2(my_panel;\n    yname      = :Y_post,\n    yname_pre  = :Y_pre,\n    treat      = :W,\n    exposure   = :G,\n    g          = 1,\n    covariates = [:z1, :z2],\n    trim       = 0.01,\n)\nprintln(res.estimate)   # DR direct ATT at G == 1\nprintln(res.ci)         # 95% CI\n\nSee the vignettes in the sidebar for examples and the Reference page for the API.\n\nThe companion R package didint ships a Brazil Amazon Priority List replication vignette (Xu 2026 Section III). The two packages use the same DR formula and trim / aggregation behaviour, so estimates match to MC noise.","title":"At a glance"},{"category":"section","location":"vignettes/02_staggered/#Staggered-Adoption","page":"Staggered Adoption","text":"This vignette demonstrates did_int_staggered on a multi-cohort panel where treatment is adopted at different times across units. The DR DATT is computed per (cohort, time) cell, then aggregated across cells with joint-IF stacking that correctly accounts for the shared not-yet-treated comparison group.\n\nusing DidInterference\nusing DataFrames\nusing Random\nusing Statistics\n\nfunction simulate_staggered(; N = 1500, T = 5, seed = 7)\n    Random.seed!(seed)\n    lon  = rand(N) .* 10\n    lat  = rand(N) .* 10\n    z    = 0.3 .* lon .+ 0.2 .* lat .+ randn(N)\n    p_t  = 1 ./ (1 .+ exp.(0.5 .- 0.5 .* z))\n    is_t = rand(N) .< p_t\n    cohort = fill(Inf, N)\n    cohort[is_t] = rand([2, 3, 4], sum(is_t))\n    dij = [sqrt((lon[i]-lon[j])^2 + (lat[i]-lat[j])^2) for i in 1:N, j in 1:N]\n    A   = (dij .< 1.5) .& (dij .> 0)\n    deg = max.(sum(A, dims = 2)[:], 1)\n    rows = NamedTuple[]\n    for i in 1:N, t in 1:T\n        W_t = Int(cohort[i] <= t)\n        share_t = sum(A[i, :] .* (cohort .<= t)) / deg[i]\n        G_t = Int(share_t > 0.3)\n        Y = 0.8 * z[i] + 0.1 * t * z[i] +\n            1.5 * W_t + 0.5 * G_t * W_t + randn()\n        push!(rows, (id = i, time = t, cohort = cohort[i],\n                     z = z[i], Y = Y, G = G_t))\n    end\n    DataFrame(rows)\nend\n\npanel = simulate_staggered(N = 1500, seed = 7)\ncombine(groupby(panel, :cohort), nrow => :n_obs)","title":"Staggered Adoption"},{"category":"section","location":"vignettes/02_staggered/#Fit-the-staggered-estimator","page":"Staggered Adoption","text":"res = did_int_staggered(panel;\n    yname     = :Y,\n    time      = :time,\n    id        = :id,\n    cohort    = :cohort,\n    exposure  = :G,\n    g         = 1,\n    covariates = [:z],\n)\n\nselect(res.per_cell,\n       [:cohort, :time, :event_time, :estimate, :se, :ci_lo, :ci_hi])\n\nEach row is a (c, t) cell. The true direct + spillover effect at G = 1 is 2.0; estimates should be close.","title":"Fit the staggered estimator"},{"category":"section","location":"vignettes/02_staggered/#Aggregates","page":"Staggered Adoption","text":"res.agg.simple\n\nThe simple cross-cell average uses sample-size weights w_k = n_k  sum_l n_l. Its standard error is the joint-IF stacked variance across units that appear in multiple cells — not the independent-cells approximation, which underestimates the true variance when cells share the never-treated comparison group.\n\nBy event time:\n\nres.agg.event_time\n\nBy cohort:\n\nres.agg.cohort","title":"Aggregates"},{"category":"section","location":"vignettes/02_staggered/#Why-joint-IF-aggregation-matters","page":"Staggered Adoption","text":"In a small Monte Carlo with the same DGP, the independent-cells SE gave 84 % CI coverage; the joint-IF stacking restores it to 96 %. The R companion package didint reports the same comparison in its inst/sims/findings.md.\n\nFor an applied example, see the R didint vignette on the Brazil Amazon Priority List. It fits the staggered-DiD-with-interference design on the Assunção et al. (2023) data.","title":"Why joint-IF aggregation matters"},{"category":"section","location":"vignettes/03_multiplicative/#Multiplicative-DiD-for-Count-Outcomes","page":"Count Outcomes","text":"Count outcomes — events per plant, crimes per district, hospital admissions per county — require care. The additive parallel trends assumption asks whether the level change would have been the same for treated and control units. But for counts, a proportional model is more natural: units that start with twice as many events tend to gain twice as many events under a parallel shock.\n\nThere is a second problem. When zeros are common, the workaround log(1 + Y) is not scale-invariant: multiplying Y by a constant shifts the estimated effect, so the \"effect\" depends on the unit of measurement. Chen and Roth (2024) recommend using a multiplicative (Poisson-based) model instead.","title":"Multiplicative DiD for Count Outcomes"},{"category":"section","location":"vignettes/03_multiplicative/#Estimand","page":"Count Outcomes","text":"Under multiplicative parallel trends, the causal estimand at exposure level g is\n\ntheta(g) = fracEY_1(1g)  EY_0(1g)EY_1(0g)  EY_0(0g) - 1\n\nThis is the ratio of the treated group's growth ratio to the control group's counterfactual growth ratio, minus one. When θ(g) = 0.40, treated units at exposure g have 40 % more events than their multiplicative-parallel-trends counterfactual. The estimand is identified by the four AIPW means, each fit with a Poisson quasi-MLE outcome model.\n\nPass family = :poisson to any of the three estimators to get this multiplicative ATT. The default family = :gaussian is the existing additive estimator.","title":"Estimand"},{"category":"section","location":"vignettes/03_multiplicative/#Example:-staggered-adoption-with-count-outcomes","page":"Count Outcomes","text":"using DidInterference\nusing DataFrames\nusing Random\nusing Distributions\nusing Statistics\n\nfunction simulate_count_staggered(; N = 1500, T = 5, seed = 42)\n    Random.seed!(seed)\n    z      = randn(N)\n    p_t    = 1 ./ (1 .+ exp.(0.5 .- 0.5 .* z))\n    is_t   = rand(N) .< p_t\n    cohort = fill(Inf, N)\n    cohort[is_t] = rand([2.0, 3.0, 4.0], sum(is_t))\n\n    rows = NamedTuple[]\n    for i in 1:N, t in 1:T\n        W_t = Int(cohort[i] <= t)\n        G_t = rand() < 0.4 ? 1 : 0\n        lam = exp(0.5 + 0.3 * z[i] + 0.3 * W_t + 0.35 * G_t * W_t)\n        Y   = float(rand(Poisson(lam)))\n        push!(rows, (id = i, time = t, cohort = cohort[i],\n                     z = z[i], Y = Y, G = G_t))\n    end\n    DataFrame(rows)\nend\n\npanel = simulate_count_staggered(N = 1500, seed = 42)\ncombine(groupby(panel, :cohort), nrow => :n_obs)","title":"Example: staggered adoption with count outcomes"},{"category":"section","location":"vignettes/03_multiplicative/#Fit-the-multiplicative-staggered-estimator","page":"Count Outcomes","text":"res = did_int_staggered(panel;\n    yname      = :Y,\n    time       = :time,\n    id         = :id,\n    cohort     = :cohort,\n    exposure   = :G,\n    g          = 1,\n    covariates = [:z],\n    family     = :poisson,\n)\n\nselect(res.per_cell,\n       [:cohort, :time, :event_time, :estimate, :se, :ci_lo, :ci_hi])\n\nEach row is a (cohort, time) cell. The true multiplicative ATT at G = 1 is exp(0.35) − 1 ≈ 0.419; units treated at exposure g = 1 have roughly 42 % more events than their multiplicative-parallel-trends counterfactual.","title":"Fit the multiplicative staggered estimator"},{"category":"section","location":"vignettes/03_multiplicative/#Aggregates","page":"Count Outcomes","text":"res.agg.simple\n\nBy event time:\n\nres.agg.event_time\n\nBy cohort:\n\nres.agg.cohort","title":"Aggregates"},{"category":"section","location":"vignettes/03_multiplicative/#Reference","page":"Count Outcomes","text":"Chen, J., and Roth, J. (2024). \"Logs with Zeros? Some Problems and Solutions.\" Quarterly Journal of Economics 139(2): 891–936.","title":"Reference"}]
}
